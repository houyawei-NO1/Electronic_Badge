# 电子吧唧程序逻辑设计文档

## 1. 硬件配置

### 1.1 GPIO定义

> **硬件版本说明**：
> - **调试版(经典款)**：带串口芯片(CH340/CP2102)，GPIO20被串口占用，使用GPIO0作为SPI_SCK
> - **量产版(简约款)**：不带串口芯片，GPIO20可用作SPI_SCK
> - 当前配置为**调试版**，量产时请修改 `GPIO_SPI_SCK` 为 `20`

| 功能 | GPIO | 说明 | 版本 |
|------|------|------|------|
| SPI_SCK (SCL) | **GPIO0** | 屏幕SPI时钟 | 调试版 |
| SPI_SCK (SCL) | GPIO20 | 屏幕SPI时钟 | 量产版 |
| SPI_MOSI (SDA) | GPIO10 | 屏幕SPI数据 | 通用 |
| LCD_DC | GPIO7 | 屏幕数据/命令选择 | 通用 |
| LCD_CS | GPIO6 | 屏幕片选 | 通用 |
| LCD_RST | GPIO5 | 屏幕复位（同时控制背光供电） | 通用 |
| LCD_BL | — | 未引出到ESP32-C3（由RST控制背光） | 通用 |
| BTN_WAKE | GPIO4 | 唤醒/休眠键，GPIO唤醒源 | 通用 |
| BTN_REFRESH | GPIO3 | 刷新/配网键 | 通用 |

> **注意**：RST（GPIO5）同时控制屏幕复位和背光供电。

### 1.2 唤醒源配置
- **GPIO唤醒**: GPIO4 (WAKE键)，低电平触发（使用 `esp_deep_sleep_enable_gpio_wakeup()`）
- **TIMER唤醒**: 可配置间隔（默认30分钟，通过SDK配置编辑器设置）

---

## 2. 状态机设计

```
┌─────────────────────────────────────────────────────────────────┐
│                        上电/复位                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  【INIT】初始化阶段                                              │
│  1. 初始化NVS                                                   │
│  2. 初始化GPIO（按键）                                          │
│  3. 读取唤醒原因                                                 │
│  4. 根据唤醒原因跳转                                             │
└─────────────────────────────────────────────────────────────────┘
          │
          ├─────────────────────┬─────────────────────┐
          ▼                     ▼                     ▼
┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐
│  TIMER_WAKEUP   │   │   GPIO_WAKEUP   │   │  POWERON_RESET  │
│   (定时更新)     │   │   (按键唤醒)     │   │   (首次上电)     │
└────────┬────────┘   └────────┬────────┘   └────────┬────────┘
         │                     │                     │
         ▼                     ▼                     ▼
┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐
│ 【UPDATE】更新模式 │   │【DISPLAY】显示模式│   │ 【DISPLAY】显示模式│
└─────────────────┘   └─────────────────┘   └─────────────────┘
```

---

## 3. 各模式详细逻辑

### 3.1 【INIT】初始化阶段

```c
void init_phase() {
    // 1. 初始化NVS
    nvs_flash_init();
    
    // 2. 初始化GPIO
    gpio_config_t io_conf = {};
    // 配置按键输入（内部上拉）
    // 配置RST输出（默认低电平，关闭屏幕和背光）
    
    // 3. 读取唤醒原因
    esp_sleep_wakeup_cause_t cause = esp_sleep_get_wakeup_cause();
    
    // 4. 根据原因跳转
    switch(cause) {
        case ESP_SLEEP_WAKEUP_TIMER:
            enter_update_mode();
            break;
        case ESP_SLEEP_WAKEUP_GPIO:  // GPIO唤醒（WAKE键）
            enter_display_mode();
            break;
        default: // POWERON_RESET
            enter_display_mode();
            break;
    }
}
```

---

### 3.2 【DISPLAY】显示模式

**进入条件**: GPIO唤醒 或 首次上电

**功能**: 显示主屏幕（时间+天气壁纸）

**流程**:
```
1. 初始化SPI总线
2. 初始化GC9A01屏幕
3. 拉高RST (GPIO5 = HIGH) 开启屏幕和背光供电
4. 加载并显示天气壁纸
   - 优先显示AI生成的天气图片
   - 如果没有AI图片，显示内置默认天气图片
5. 在角落显示时间
6. 显示上次天气更新时间
7. 启动按键监听任务
8. 启动休眠倒计时（如60秒无操作）
```

**按键处理**:

| 按键 | 操作 | 动作 |
|------|------|------|
| WAKE | 短按 | 无动作（已在显示模式） |
| WAKE | 长按(>2秒) | 进入休眠 |
| REFRESH | 短按 | 进入【REFRESH】刷新模式 |
| REFRESH | 长按(>3秒) | 进入【CONFIG】配网模式 |

> **注意**：按键检测使用中断+消抖，长按事件在达到阈值时触发一次，不会重复触发

**退出条件**:
- 长按WAKE → 进入【SLEEP】休眠
- 短按REFRESH → 进入【REFRESH】刷新模式
- 长按REFRESH → 进入【CONFIG】配网模式
- 超时无操作(60秒) → 进入【SLEEP】休眠

---

### 3.3 【UPDATE】更新模式

**进入条件**: TIMER唤醒

**功能**: 后台更新天气数据，可选生成AI图片

**流程**:
```
1. 初始化WiFi（STA模式）
2. 尝试连接WiFi（按优先级）
   - **优先尝试sdkconfig默认配置**（CONFIG_WIFI_SSID/CONFIG_WIFI_PASSWORD，开发调试用）
   - sdkconfig未设置或连接失败时，尝试NVS保存的配置（SmartConfig配网，最多5组）
   - 连接成功则继续，失败则放弃本次更新
3. NTP同步时间
4. 获取天气数据（和风天气API）
5. 保存天气数据到RTC Memory
6. 【并行】生成AI图片（Pollinations.AI）
   - 构建prompt：天气+节日+风格
   - 请求图片生成（最大等待60秒）
   - 成功：保存图片到SPIFFS
   - 失败：使用默认天气图片
7. 关闭WiFi
8. 更新屏幕显示（如果当前在显示模式）
9. 进入【SLEEP】休眠
```

**注意**: 此模式下屏幕不点亮，纯后台操作

---

### 3.4 【REFRESH】刷新模式

**进入条件**: 在显示模式下短按REFRESH键

**功能**: 立即更新天气数据

**流程**:
```
1. 显示"Updating..."加载提示
2. 连接WiFi（按优先级：NVS配置 → sdkconfig默认配置）
3. 同步NTP时间
4. 获取最新天气（和风天气API）
5. 生成AI图片（Pollinations.AI）
6. 更新RTC Memory中的天气数据
7. 刷新屏幕显示（时间+天气+温度）
8. 断开WiFi
9. 回到显示模式，重置60秒休眠倒计时
```

**错误处理**:
- WiFi连接失败：显示"WiFi Failed"，1秒后恢复之前显示
- 天气获取失败：显示"Update Failed"，1秒后恢复之前显示
- 成功更新后：立即显示最新数据

---

### 3.5 【CONFIG】配网模式

**进入条件**: 在显示模式下长按REFRESH键(>3秒)

**功能**: 配置WiFi连接

**流程**:
```
1. 显示"Config Mode"提示
2. 启动SmartConfig (一键配网)
3. 等待手机配置（使用微信小程序"一键配网"）
4. 显示动态进度 "Configuring..."（每2秒更新）
5. 收到配置后：
   - 自动尝试连接新WiFi
   - 连接成功：保存到NVS（支持最多5组配置）
   - 显示"Config Success"
   - 自动进入UPDATE模式获取天气
6. 退出方式：
   - 配网成功 → 自动更新天气 → 进入SLEEP
   - 长按WAKE键(>2秒) → 退出配网 → 回到显示模式
   - 超时(5分钟) → 显示"Timeout" → 回到显示模式
```

**使用说明**:
- 打开微信小程序搜索"一键配网"
- 确保手机连接目标WiFi
- 选择"SmartConfig配网"模式
- 输入WiFi密码，点击"一键配网"
- 设备会自动接收配置并连接
- 也可使用其他支持SmartConfig的小程序（如"SmartConfig配网"）

**WiFi配置优先级**:

| 优先级 | 配置来源 | 使用场景 | 说明 |
|--------|---------|---------|------|
| 1 | **sdkconfig默认配置** | 开发调试/固定环境 | 在SDK配置编辑器中设置，优先使用 |
| 2 | **NVS保存的配置** | 正常使用/多环境切换 | SmartConfig配网后保存，支持最多5组 |

**连接逻辑**:
```
wifi_connect()
  ├─ 1. 使用sdkconfig默认配置（CONFIG_WIFI_SSID/CONFIG_WIFI_PASSWORD）
  │   ├─ 已设置？→ 尝试连接
  │   └─ 连接成功？→ 返回成功
  │   └─ 连接失败？→ 继续
  └─ 2. 遍历NVS保存的配置（最多5组）
      ├─ 有有效配置？→ 尝试连接
      └─ 无有效配置？→ 返回失败
```

**启动时WiFi判断逻辑**:
```
app_main()
  ├─ 检查WiFi配置来源
  │   ├─ sdkconfig已设置？→ 有WiFi配置
  │   └─ NVS有配置？→ 有WiFi配置
  │   └─ 都没有？→ 进入配网模式
  │
  ├─ 首次启动（boot_count == 1）→ 进入UPDATE模式获取天气
  └─ 非首次启动 → 检查数据新鲜度
      ├─ 数据过期（>1小时未更新）→ 进入UPDATE模式
      └─ 数据新鲜 → 进入DISPLAY模式
```

**WiFi配置存储结构**:
```c
typedef struct {
    char ssid[32];
    char password[64];
    uint8_t valid;  // 标记此配置是否有效
} wifi_config_t;

// NVS中保存最多5组配置
wifi_config_t saved_configs[5];
```

---

### 3.6 【SLEEP】休眠模式

**进入条件**: 
- 显示模式超时
- 长按WAKE键
- 更新模式完成

**流程**:
```
1. 拉低RST (GPIO5 = LOW) 关闭屏幕和背光供电
2. 关闭SPI总线
3. 配置GPIO唤醒源（GPIO4，低电平）
4. 配置TIMER唤醒源（通过CONFIG_UPDATE_INTERVAL_MINUTES设置，默认30分钟）
5. 保存必要状态到RTC Memory
6. 进入Deep Sleep
```

---

## 4. 数据存储设计

### 4.1 NVS存储（Flash，持久保存）

| 键名 | 类型 | 说明 |
|------|------|------|
| wifi_config_0~4 | struct | 5组WiFi配置 |
| timezone | int8 | 时区偏移（预留，当前在代码中硬编码为 Asia/Shanghai）|

### 4.2 RTC Memory（Deep Sleep保持）

| 变量 | 类型 | 说明 |
|------|------|------|
| last_weather | struct | 上次天气数据 |
| last_update_time | time_t | 上次更新时间 |
| has_ai_image | bool | 是否有AI生成图片 |
| boot_count | uint32 | 启动次数 |

---

## 5. 图片管理

### 5.1 内置默认图片（编译到固件）

| 文件名 | 描述 |
|--------|------|
| weather_sunny.c | 晴天 |
| weather_cloudy.c | 多云 |
| weather_rainy.c | 雨天 |
| weather_snowy.c | 雪天 |
| weather_thunder.c | 雷雨 |
| weather_foggy.c | 雾天 |

### 5.2 AI生成图片（SPIFFS文件系统）

- 路径: `/spiffs/ai_weather.jpg`
- 每次生成新图片覆盖旧文件
- 分辨率: 240x240

### 5.3 图片显示优先级

```
if (SPIFFS存在AI图片 && AI图片新鲜) {
    显示AI图片
} else {
    根据天气代码显示默认图片
}
```

---

## 6. AI图片生成Prompt设计

### 6.1 Prompt模板

```
电子吧唧风格，圆形构图，[天气描述]，[节日元素]，[时间元素]，
可爱卡通风格，柔和色彩，适合圆形屏幕显示，高清
```

### 6.2 示例

| 场景 | Prompt |
|------|--------|
| 晴天日常 | 电子吧唧风格，圆形构图，阳光明媚的蓝天，白云朵朵，可爱卡通风格，柔和色彩，适合圆形屏幕显示，高清 |
| 雨天端午 | 电子吧唧风格，圆形构图，细雨绵绵的江南水乡，粽子元素，龙舟装饰，可爱卡通风格，柔和色彩，适合圆形屏幕显示，高清 |
| 雪天春节 | 电子吧唧风格，圆形构图，雪花纷飞的冬日景象，红灯笼，春联元素，可爱卡通风格，柔和色彩，适合圆形屏幕显示，高清 |

---

## 7. 主屏幕UI布局

```
┌─────────────────────────────┐
│                             │
│      [天气壁纸图片]          │
│      240x240 全屏显示        │
│                             │
│  ┌─────────────────────┐    │
│  │    14:30            │    │
│  │    6月2日 周一       │    │
│  └─────────────────────┘    │
│                             │
│  上次更新: 10:15            │
│                             │
└─────────────────────────────┘
```

- 时间显示：屏幕中央或上方
- 日期显示：时间下方
- 上次更新时间：屏幕底部小字

---

## 8. 错误处理

| 错误场景 | 处理方式 |
|----------|----------|
| WiFi连接失败 | 显示"WiFi未连接"，使用RTC缓存数据 |
| 天气API失败 | 显示"天气获取失败"，使用RTC缓存数据 |
| AI生成超时 | 放弃本次生成，使用默认图片 |
| SPIFFS写入失败 | 记录错误，使用默认图片 |
| 按键抖动 | 软件消抖（10nF硬件+10ms软件） |

---

## 9. 功耗优化策略

1. **Deep Sleep**: 主休眠模式，仅RTC运行
2. **屏幕和背光控制**: RST (GPIO5) 控制屏幕和背光供电，休眠时拉低关闭
3. **WiFi及时关闭**: 更新完成后立即关闭
4. **SPI总线管理**: 休眠前关闭
5. **AI生成超时**: 60秒超时避免长时间耗电

---

## 10. 配置说明

### SDK配置（menuconfig）

所有关键配置都可通过 `idf.py menuconfig` 或 SDK配置编辑器设置：

| 配置项 | 路径 | 说明 |
|--------|------|------|
| **WiFi SSID** | Electronic Badge Configuration → WiFi Configuration | 默认WiFi名称 |
| **WiFi Password** | Electronic Badge Configuration → WiFi Configuration | 默认WiFi密码 |
| **天气API Host** | Electronic Badge Configuration → Weather API Configuration | 和风天气API Host（控制台查看）|
| **天气API Key** | Electronic Badge Configuration → Weather API Configuration | 和风天气API密钥 |
| **Location Code** | Electronic Badge Configuration → Weather API Configuration | 城市代码（默认杭州101210101）|
| **更新间隔** | Electronic Badge Configuration → Weather API Configuration | 天气更新间隔（分钟）|

**天气API配置获取**：
1. 访问 https://dev.qweather.com/ 注册账号
2. 创建项目并获取 **API Host** 和 **API Key**
3. 在控制台 → 设置中查看你的专属 **API Host**（格式：xxx.qweatherapi.com）
4. 在SDK配置编辑器中填入 **API Host** 和 **API Key**

### 配置优先级

| 配置项 | 代码默认值 | sdkconfig | 运行时 |
|--------|-----------|-----------|--------|
| WiFi SSID | 空 | CONFIG_WIFI_SSID | NVS保存 > sdkconfig |
| WiFi Password | 空 | CONFIG_WIFI_PASSWORD | NVS保存 > sdkconfig |
| 天气API Host | devapi.qweather.com | CONFIG_WEATHER_API_HOST | sdkconfig |
| 天气API Key | 空 | CONFIG_WEATHER_API_KEY | sdkconfig |
| 城市代码 | 101210101 | CONFIG_WEATHER_LOCATION | sdkconfig |
| 更新间隔 | 30分钟 | CONFIG_UPDATE_INTERVAL_MINUTES | sdkconfig |
| AI图片开关 | 启用 | CONFIG_AI_IMAGE_ENABLED | sdkconfig |
| AI图片超时 | 60秒 | CONFIG_AI_IMAGE_TIMEOUT_MS | sdkconfig |
| AI图片宽度 | 240 | CONFIG_AI_IMAGE_WIDTH | sdkconfig |
| AI图片高度 | 240 | CONFIG_AI_IMAGE_HEIGHT | sdkconfig |
| 显示超时 | 60秒 | CONFIG_DISPLAY_TIMEOUT_SECONDS | sdkconfig |
| SPI时钟 | 40MHz | CONFIG_SPI_CLOCK_MHZ | sdkconfig |
| 调试模式 | 关闭 | CONFIG_DEBUG_MODE | sdkconfig |
| 日志级别 | Info(3) | CONFIG_DEBUG_LOG_LEVEL | sdkconfig |

## 12. 代码模块划分

```
main/
├── main.c              // 主程序入口
├── Kconfig.projbuild   // 菜单配置（SDK配置编辑器）
├── CMakeLists.txt
├── include/
│   ├── config.h        // 全局配置（从sdkconfig读取）
│   ├── display.h       // 显示模块
│   ├── wifi_manager.h  // WiFi管理
│   ├── weather.h       // 天气API
│   ├── ai_image.h      // AI图片生成
│   ├── button.h        // 按键处理
│   └── power.h         // 电源管理
├── src/
│   ├── display.c       // GC9A01驱动+UI
│   ├── wifi_manager.c  // WiFi连接+SmartConfig
│   ├── weather.c       // 和风天气API
│   ├── ai_image.c      // Pollinations.AI
│   ├── button.c        // 按键中断+消抖
│   └── power.c         // Deep Sleep管理
└── resources/
    ├── fonts/          // 字库
    └── images/         // 默认天气图片
```

---

## 13. 待确认事项

- [x] WAKE键长按休眠逻辑
- [x] REFRESH键短按刷新、长按配网
- [x] 多WiFi配置保存
- [x] 更新间隔可通过sdkconfig配置（默认30分钟）
- [x] 每次更新都生成AI图片，60秒超时
- [x] 天气失败显示上次数据+时间
- [x] 内置默认天气图片
- [x] gzip解压（使用ESP32-C3 ROM miniz）
- [x] HTTPS请求（跳过证书CN检查）
- [ ] 字库选择（推荐使用开源中文字库，如需要中文显示）
- [ ] 是否添加电池电量检测（需要硬件支持ADC分压电路）
- [ ] AI生成图片的JPEG解码和显示（当前仅保存到SPIFFS，未实现屏幕显示）
