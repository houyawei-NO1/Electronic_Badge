# 电子吧唧程序逻辑设计文档

## 1. 硬件配置

### 1.1 板子选型

| 型号 | ESP32-C3 Super Mini | 合宙 ESP32-C3 开发板 |
|------|---------------------|----------------------|
| 天线 | PCB 陶瓷天线（默认），需**飞线焊接外接天线**以获得稳定信号 | 板载天线 |
| 串口 | USB 直连（无串口芯片） | CP2102/CH340 串口芯片 |
| Flash | 4MB | 4MB |
| 适用场景 | 产品尺寸敏感 / 嵌入式安装 | 开发调试 |

### 1.2 GPIO 定义

> **SPI SCK 说明**：当前项目已切换为 GPIO20（Super Mini 的默认 SPI SCK），合宙开发板需改回 GPIO0。

| 功能 | GPIO | 说明 |
|------|------|------|
| SPI_SCK (SCL) | **GPIO20** | 屏幕 SPI 时钟 |
| SPI_MOSI (SDA) | GPIO10 | 屏幕 SPI 数据 |
| LCD_DC | GPIO7 | 屏幕数据/命令选择 |
| LCD_CS | GPIO6 | 屏幕片选 |
| LCD_RST | GPIO5 | 屏幕复位（同时控制背光供电） |
| LCD_BL | — | 未引出到 ESP32-C3（由 RST 控制，**软件无法关闭背光**） |
| BTN_WAKE | GPIO4 | 唤醒/休眠键，GPIO 唤醒源 |
| BTN_REFRESH | GPIO3 | 刷新/配网键 |

> **重要硬件限制**：RST（GPIO5）同时控制屏幕复位和背光供电。低电平关闭屏幕和背光，但这也意味着屏幕和背光绑定在一起——无法单独关背光保屏幕。

### 1.3 天线信号实测（关键结论）

测试条件：同一位置、同一路由器（TP-LINK_D975, ch1）、未修改软件

| 设备 | RSSI | 连接表现 |
|------|------|----------|
| 合宙开发板（板载天线） | **-63 dBm** | 秒连，稳定 |
| Super Mini（陶瓷天线） | **-82 dBm** | 认证超时/丢包，反复重连 |
| Super Mini（**陶瓷天线飞线焊接铜线后**） | **-59 dBm** | 秒连，稳定 |

**约 20 dB 的信号改善**，结论：Super Mini 原厂陶瓷天线性能不足以穿墙使用。

---

## 2. 状态机设计

```
┌─────────────────────────────────────────────────────────────────┐
│                        上电/复位                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  【INIT】初始化阶段                                              │
│  1. 初始化 NVS                                                    │
│  2. 无条件设置时区 (TZ=CST-8)                                    │
│  3. 读取唤醒原因                                                 │
│  4. 初始化显示（SPI + GC9A01 + LVGL）                           │
│  5. 根据唤醒原因跳转                                             │
└─────────────────────────────────────────────────────────────────┘
          │
          ├─────────────────────┬─────────────────────┐
          ▼                     ▼                     ▼
┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐
│  TIMER_WAKEUP   │   │   GPIO_WAKEUP   │   │  POWERON_RESET  │
│   (定时更新)     │   │   (按键唤醒)     │   │   (首次上电)     │
└────────┬────────┘   └────────┬────────┘   └────────┬────────┘
         │                     │                     │
         ▼                     ▼                     ▼
┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐
│ 【UPDATE】更新模式 │   │【DISPLAY】显示模式│   │ 【DISPLAY】显示模式│
└─────────────────┘   └─────────────────┘   └─────────────────┘
```

---

## 3. 各模式详细逻辑

### 3.1 【INIT】初始化阶段

```c
void app_main() {
    nvs_flash_init();

    // 无条件设置时区（CST-8 = UTC+8），TZ 环境变量在 deep sleep 后会丢失
    setenv("TZ", "CST-8", 1);
    tzset();
    // 若 RTC 时钟在休眠后未恢复，用上次天气更新时间兜底
    // (详见 main.c 中的 system_time_valid() / set_system_time_from_epoch() )

    esp_sleep_wakeup_cause_t cause = esp_sleep_get_wakeup_cause();
    // 按唤醒原因跳转（TIMER → UPDATE，GPIO/POWERON → DISPLAY）
}
```

### 3.2 【DISPLAY】显示模式

**进入条件**：GPIO 唤醒 或 首次上电 / RTC 数据有效

**功能**：显示主屏幕（纯黑背景 + 时间 + 天气图标 + 天气名 + 温度 + 湿度/风力 + 更新时间）

**UI 布局**：

```
┌─────────────────────────────┐
│                             │
│        10:35                │  ← 时间（白色，Montserrat 14px）
│                             │
│      [多云图标 ← 顺时针90°修正]│  ← 天气图标（动画: 云左右移动）
│                             │
│         26°C                │  ← 温度（白色，思源黑体 16px，在天气下 2px）
│                             │
│    Hum:50% Wind:4           │  ← 细节信息
│                             │
│  2026.6.9 10:35更新         │  ← 更新时间（灰色，呼吸灯动画 1s 周期）
│                             │
└─────────────────────────────┘
  背景：纯黑 #000000
```

**显示技术栈**：
- **驱动**：`esp_lcd` + `esp_lcd_gc9a01`（SPI 接口）
- **GUI**：`esp_lvgl_port` + **LVGL v8.3.x**（非 v9，注意版本）
- **字体**：`lv_font_montserrat_14/24/28/48`（数字/英文）+ `lv_font_simsun_16_cjk`（中文子集）。**凡是包含中文字符（"更新中"、"同步中"、天气描述、更新时间等）的 label 必须显式设置为 `simsun_16_cjk` 字体，否则会显示方框**。
- **颜色格式**：RGB565，`CONFIG_LV_COLOR_16_SWAP=y`（LVGL 侧做字节交换）+ `rgb_endian = LCD_RGB_ENDIAN_RGB`（驱动侧设置 MADCTL RGB 位）。**面板初始化后必须调用 `esp_lcd_panel_invert_color(panel, true)` 来翻转显示极性**（GC9A01 默认极性反了，会导致黑底白字反相）。如果写成 `LCD_RGB_ENDIAN_BGR` 或未开启反转，结果都是颜色/极性反了。
- **缓冲**：单缓冲 10% 屏幕大小
- **`display_init()` 内部顺序**：① SPI 初始化 → ② GC9A01 panel 初始化（`esp_lcd_panel_invert_color(panel, true)` 必须调用） → ③ `lvgl_port_init`（启动 LVGL task）→ ④ `lvgl_port_add_disp` 注册显示 → ⑤ SPIFFS 挂载 → ⑥ **`lvgl_port_lock` → `ui_init()` → 设置所有 label 的白色字体/中文 simsun_16_cjk → `lvgl_port_unlock`**（⑥ 全在锁内，因为 ui_init 调用 LVGL 对象创建 API，必须在 LVGL task 启动后持锁执行，否则对象树会损坏导致黑屏）

**天气图标旋转修正**：和风天气 PNG 素材本身顺时针旋转了 90°，代码里对 icon code 101 做 `lv_img_set_angle(img, 2700)`（逆时针 90°）。若发现其他天气代码的图标同样旋转错位，在 `icon_rotation_correction()` 中加入即可。

**天气图标预解码机制（重写后，彻底绕过 LVGL decoder）**：`display_main_screen()` **在 `lvgl_port_lock` 之前**调用 `display_prepare_weather_icon(weather_code)`。`display_prepare_weather_icon` 不再通过 `lv_img_decoder_open`/`lv_mem_alloc`（这两个 API 要求在 LVGL 锁内调用，长时间持锁会阻塞 LVGL task 并可能导致黑屏/看门狗），而是**直接通过标准 C 库 `fopen/fread/malloc` 读取 SPIFFS 中的 PNG 文件，调用 `lodepng_decode32` 解码为 RGBA8888，再手工转换为 RGB565+alpha（每像素 3 字节），最后构造 `lv_img_dsc_t` 指向这个 malloc 的 buffer**。`lv_color_make(r, g, b)` 是 `static inline` 纯位操作函数，不访问任何 LVGL 全局状态，因此可以在锁外安全调用。进入 `lvgl_port_lock` 后，`lv_img_set_src(obj, &cached_weather_icon.dsc)` 只做一次指针赋值，耗时 ≈0，不会阻塞 LVGL task。解码结果以 `lv_img_dsc_t` 形式保存在 RAM（RGB565+alpha buffer），之后 draw 阶段零解码，彻底消除 task_wdt 风险。缓存带 code 记忆（相同 code 连续切换不重复解码），切屏 `lv_anim_del(NULL, NULL)` 只清理动画不清理缓存，`display_deinit` 用 `free()` 释放。

**黑屏根因与修复**：之前代码在 `lvgl_port_lock` 内部调用 `lv_img_decoder_open`（PNG 解码）+ `lv_mem_alloc`，这两个 LVGL 内部 API 会在持锁期间长时间阻塞 LVGL task（`lv_timer_handler` 无法运行），导致屏幕刷新/动画/对象状态全部停滞，外观表现为黑屏或初始状态。此外 `ui_init()` 之前在 `lvgl_port_init` 启动 LVGL task 之后、持锁之前被调用，`lv_obj_create` 等 API 在无锁状态下与运行中的 LVGL task 竞态，会导致对象树损坏、内容无法渲染。修复要点：① PNG 解码完全在 LVGL 锁外完成（lodepng + malloc）；② `ui_init()` 移入 `lvgl_port_lock/unlock` 保护段内；③ 仅 `lv_img_set_src`/`lv_obj_set_style_*` 等快操作留在锁内。

**lodepng 说明**：`lodepng_decode32(unsigned char** out, unsigned* w, unsigned* h, const unsigned char* in, size_t insize)` 已由 `managed_components/lvgl__lvgl/src/extra/libs/png/lv_png.c` 编译进固件，display.c 通过 `extern` 声明直接调用，不需要额外编译组件。

**加载界面动画**：`display_loading()` 中 `objects.arc_1` 采用**两阶段动画**：**Phase 1** 起点固定在 270°（12 点钟方向），终点从 270°→630° 逐渐填充（3 秒，单次）；**Phase 2** 填满后通过 `arc_phase1_ready_cb` 自动切换为 60° 短弧绕圆心无限旋转（3 秒/圈）。**任何切屏函数第一件事是 `lv_anim_del(NULL, NULL)` 清理所有正在运行的动画**，防止已删除对象上的动画继续触发重绘（这是 taskLVGL 看门狗触发的另一根因）。**天气图标缓存的释放**：因为 buffer 来自 `malloc`（不是 `lv_mem_alloc`），所以 `display_deinit()` 中用 `free()` 释放 cached_weather_icon.dsc.data。

**按键处理**：

| 按键 | 操作 | 动作 |
|------|------|------|
| WAKE | 短按 | 无动作（已在显示模式） |
| WAKE | 长按(>2秒) | 进入休眠 |
| REFRESH | 短按 | 进入【REFRESH】刷新模式 |
| REFRESH | 长按(>3秒) | 进入【CONFIG】配网模式 |

**退出条件**：
- 长按 WAKE → 进入【SLEEP】休眠
- 短按 REFRESH → 进入【REFRESH】刷新模式
- 长按 REFRESH → 进入【CONFIG】配网模式
- 超时无操作(60秒) → 进入【SLEEP】休眠

### 3.3 【UPDATE】更新模式

**进入条件**：TIMER 唤醒 或 首次启动 或 配网成功后

**流程**：
```
1. 初始化显示（显示加载动画）
2. 尝试连接 WiFi（按优先级）
   - 优先 sdkconfig 默认配置（CONFIG_WIFI_SSID/CONFIG_WIFI_PASSWORD）
   - sdkconfig 未设置或连接失败时，尝试 NVS 保存的配置（最多 5 组）
   - 单个配置最多重试 10 次，每次超时 20 秒
3. NTP 同步时间（3s × 2 次，失败不阻塞后续流程）
4. 显示主界面（使用 RTC 缓存数据，用户不用等待）
5. 获取天气数据（和风天气 API）
6. **若系统时间仍然不对（年份 < 2020），用天气 API 返回的 obsTime 字段修正 RTC**
7. 保存天气数据到 RTC Memory
8. 静默刷新主界面
```

> **关键优化**：NTP 经常超时/失败，不再阻塞。天气 API 响应包含 `obsTime`（ISO8601 时间戳），用它作为权威时间源设置系统时间。

### 3.4 【REFRESH】刷新模式

**进入条件**：在显示模式下短按 REFRESH 键

**流程**：
```
1. 检查 WiFi 是否已连接
   - 已连接：直接获取天气（秒级）
   - 未连接：连接 WiFi → 同步时间 → 获取天气
2. 刷新屏幕显示
3. 重置 60 秒休眠倒计时
```

### 3.5 【CONFIG】配网模式

**进入条件**：在显示模式下长按 REFRESH 键(>3秒)

**流程**：
```
1. 显示"配网模式" + 二维码
2. 启动 SmartConfig（一键配网，超时 5 分钟）
3. 收到配置后自动连接 → 保存到 NVS → 静默获取天气
4. 退出方式：
   - 配网成功 → 回到显示模式
   - 长按 WAKE 键(>2秒) → 退出配网
   - 超时(5分钟) → 回到显示模式
```

### 3.6 【LOW POWER】低功耗模式（Light Sleep）

**进入条件**：
- 显示模式超时（60秒无操作）
- 长按 WAKE 键

**流程**：
```
1. 启用自动 tickless idle light sleep (power_enable_light_sleep)
2. 显示屏保（黑色背景 + 大号时间 + "Press WAKE"）
3. 断开 WiFi（降低功耗）
4. 配置 GPIO 唤醒源（GPIO3/GPIO4，低电平）
5. 配置 TIMER 唤醒源（默认 1 分钟）
6. 进入 esp_light_sleep_start() 循环
   - Timer 唤醒 → 刷新屏保时间 → 继续睡
   - GPIO 唤醒 → 禁用 light sleep (power_disable_light_sleep) → 返回主循环
```

**与 Deep Sleep 的区别**（已废弃 deep sleep）：

| 特性 | Deep Sleep（旧） | Light Sleep（当前） |
|------|------------------|---------------------|
| RAM | 丢失（需 RTC Memory） | **保留** |
| GPIO 状态 | 复位 | **保持** |
| 屏幕内容 | 需重新初始化 | **保持可见** |
| 背光 | 会关闭（RST拉低） | **保持开启** |
| 唤醒后操作 | 重启，从 app_main 开始 | **继续执行**，回到 while(1) 循环 |
| 天气数据 | 需从 RTC Memory 恢复 | **RAM 中直接可用** |

**关键设计**：`esp_pm_configure(.light_sleep_enable = true)` **不在 `power_init()` 中调用**，而是推迟到 `enter_low_power_mode()` 才启用。这样保证：
- 启动阶段 / 显示模式 / 更新模式：CPU 160MHz 全速，UART 日志正常输出
- 低功耗屏保阶段：自动 tickless idle light sleep，省电但日志会暂停
- 按键唤醒后：`power_disable_light_sleep()` 恢复全速，日志立即恢复

> **功耗说明**：无法关背光 → 屏保功耗仍≈20-40mA（主要是背光）。Light sleep 主要节省的是 CPU 功耗。如果要真正低功耗需要硬件改动（加 MOSFET 控制背光）。

> **USB 直连板子注意**：进入 light sleep 后 USB 串口会暂停，日志输出中断。这是正常现象，按键唤醒后自动恢复。

---

## 4. 数据存储设计

### 4.1 NVS 存储（Flash，持久保存）

| 键名 | 类型 | 说明 |
|------|------|------|
| wifi_config_0~4 | struct | 5 组 WiFi 配置（SSID + Password） |
| weather::last_data | blob | **天气数据完整结构体**（weather_data_t），含天气代码、温度、湿度、风力、天气描述、更新时间等 |

**天气数据持久化逻辑**：
- `weather_save_to_nvs()`：在 `enter_update_mode()` 成功获取天气后调用，将 `weather_data_t` 完整写入 NVS
- `weather_load_from_nvs()`：在 `app_main()` 初始化阶段调用，从 NVS 恢复天气数据到 RAM
- 验证：读取时检查 `update_time > 2020-01-01`，无效数据自动丢弃
- 这样唤醒后无需重启即可直接使用缓存数据，避免每次都显示 WiFi 连接界面

> 旧方案使用 RTC Memory（deep sleep 保持），现已废弃。Light sleep 保留 RAM，但 NVS 持久化保证即使意外复位也能恢复数据。

### 4.2 RAM 数据（Light Sleep 保持）

| 变量 | 类型 | 说明 |
|------|------|------|
| `s_weather_data` | weather_data_t | 天气数据完整结构体（light sleep 后 RAM 保留） |
| `s_has_weather` | bool | 是否有有效天气数据 |
| `s_last_weather_update` | uint32_t | 上次更新的 Unix 时间戳 |
| `s_boot_count` | uint32_t | 启动/唤醒次数 |

> Light sleep 保留 RAM，所以这些数据在屏保唤醒后仍然有效。NVS 作为兜底，在意外复位时恢复。

---

## 5. 时间管理（Time）

ESP32-C3 没有硬件 RTC（只有 RTC Timer 用于唤醒），时间来源有两级：

| 来源 | 精度 | 何时使用 |
|------|------|----------|
| 天气 API `obsTime` | ±几分钟 | 主时间源（比 NTP 更稳定可用） |
| NTP | ±1秒 | 备用/校准（当前实际很少用到） |

**关键实现要点**：
1. **每次启动都 `setenv("TZ", "CST-8", 1); tzset();`**——TZ 是 RAM 变量，deep sleep 后丢失，必须重建。
2. 天气 API 成功时，若系统时间不合理（`year < 2020`），**用 `settimeofday()` 从 obsTime 推算的 epoch 设置系统时间**。
3. 所有 `localtime()` 调用都基于设置好的本地时区（UTC+8）。

---

## 6. 显示模块

### 6.1 反初始化顺序（防崩溃的关键）

```
Step 1: lv_anim_del(NULL, NULL) + lv_timer_del()  → 停止一切 LVGL 定时器
Step 2: esp_lcd_panel_del(panel_handle)            → 卸载面板驱动
Step 3: esp_lcd_panel_io_del(io_handle)            → 注销 SPI 回调（切断中断路径！）
Step 4: [delay 30ms]                                → 等已在流水线的中断退出
Step 5: lvgl_port_remove_disp(disp_handle)          → 现在才释放 LVGL 显示句柄
Step 6: lvgl_port_deinit()                          → 停止 LVGL task
Step 7: spi_bus_free(SPI2_HOST)                     → 最后释放 SPI 总线
```

**为什么这个顺序**：任何时候只要 `panel_io_del()` 还没执行，SPI `post-trans-cb` 就可能触发，并在中断上下文中调用 `lvgl_port_flush_io_ready_callback → lv_disp_flush_ready(disp_handle)`。如果那时 `disp_handle` 已经被释放，就是 **Store/Load access fault**。

> 历史上曾出现过多次因为顺序错误（先 `remove_disp` 后 `panel_io_del`，或先 `lvgl_port_deinit` 后 `panel_io_del`）导致的 Guru Meditation Error。

### 6.2 字体汇总（实际已启用）

| 字体 | 用途 | 启用 |
|------|------|------|
| lv_font_montserrat_14 | 小字/数字 | ✅ |
| lv_font_montserrat_18 | 日期/温度/详情 | ✅ |
| lv_font_montserrat_24 | - | ✅ |
| lv_font_montserrat_28 | - | ✅ |
| lv_font_montserrat_48 | 屏保时间 | ✅ |
| lv_font_simsun_16_cjk | 中文（天气/更新时间/星期等）| ✅ |

**℃ 符号显示原理**：
- 温度标签 `label_temp` 使用 **montserrat_18** 字体（非 simsun）
- montserrat_18 的生成参数包含 `-r 0xB0`（即 Unicode U+00B0 = ° 度符号）
- 代码中直接写 `"%d°C"`，UTF-8 编码 `0xC2 0xB0` 被 LVGL 解码为 U+00B0，在 montserrat_18 中命中字形
- 数字 `0-9` 和字母 `C` 也在 montserrat_18 的 `0x20-0x7F` ASCII 范围内
- 所以 `"25°C"` 完全由 montserrat_18 渲染，不需要中文字体支持

**中文星期显示**：
- 时间标签 `label_time` 使用 simsun_16_cjk 字体
- 格式：`"HH:MM  周一"` ~ `"HH:MM  周日"`
- `wday_name()` 函数将 `tm_wday` (0-6) 映射为 `"周日"`~`"周六"`
- simsun_16_cjk 字库包含 `"周"`、`"日"`~`"六"` 等字符

**中文天气描述 fallback**：
- `get_weather_name()` 先检查中文字串是否全部在 simsun_16_cjk 字库中（`font_supports_chinese()`）
- 全部支持 → 返回中文（如 `"多云"`）
- 有不支持的字 → 返回英文（如 `"Cloudy"`）
- 避免显示方框（□）

---

## 7. 错误处理与已知问题

| 错误场景 | 处理方式 |
|----------|----------|
| WiFi 连接失败 | 首次启动静默失败后直接进入显示模式（用 RTC 数据） |
| 天气 API 失败 | 使用 RTC 缓存数据，不中断显示 |
| NTP 超时 | 不阻塞，用天气 API 的 obsTime 作为系统时间 |
| 系统时间无效（年份 < 2020） | 用 RTC 中 last_update_timestamp 粗估时间 |
| 显示反初始化崩溃 | 严格遵守 §6.1 的反初始化顺序 |
| 天气图标旋转错位 | 在 `icon_rotation_correction()` 中加入修正角度 |
| **中文字体显示方框（"更新中"、"同步中"、天气描述等）** | 含中文的 label 必须显式 `lv_obj_set_style_text_font(obj, &lv_font_simsun_16_cjk, 0)`；simsun_16_cjk 仅包含项目中文常用字，不能覆盖的字符需扩充 `font_config.txt` 并重新生成字体 |
| **界面颜色反转（白背景黑文字）** | 两个独立问题：① `rgb_endian` 设为 `LCD_RGB_ENDIAN_RGB`（非 BGR），配合 `LV_COLOR_16_SWAP=y`；② **更重要：panel 初始化后必须调用 `esp_lcd_panel_invert_color(panel, true)` 来翻转显示极性（GC9A01 默认极性反了）。二者缺一都会导致颜色/极性反转 |
| **taskLVGL 看门狗触发（HuffmanTree_makeTable）** | 根因是 `lv_img_set_src(obj, "S:/xxx-fill.png")` 会在 LVGL task 的 draw 阶段同步解码 PNG，Huffman 解压缩是 CPU 密集型操作。**新方案**：在调用方 task（main task）中，**持有 lvgl_port_lock 之前**用 `display_prepare_weather_icon(code)` 预解码 PNG 到 malloc 的 RGB565+alpha buffer（绕过 LVGL decoder 和 lv_mem_alloc），之后 `lv_img_set_src(obj, &dsc)` 只做指针赋值，draw 阶段零解码。解码逻辑完全位于 LVGL 锁外，不阻塞 LVGL task。 |
| **长时间黑屏 / 不渲染内容** | 两个独立根因：① `lv_img_decoder_open` 在 LVGL 锁内执行 → LVGL task 长时间拿不到锁 → `lv_timer_handler` 不跑 → 屏幕刷新/动画全停；② `ui_init()` 在 LVGL task 已启动后无锁调用 → LVGL 对象树在创建过程中被 LVGL task 并发访问 → 对象状态损坏、内容无法渲染。修复：所有 PNG 解码移到锁外（纯 lodepng + malloc）；`ui_init()` 包裹在 `lvgl_port_lock/unlock` 内；`display_init()` 中只有 `ui_init()` 和后续 label 样式设置在锁内，SPI / panel 初始化等慢操作在锁外。 |
| **PicoPixel 导出的 UI 未带文字颜色/字体** | 不在 screens.c 中改 theme；在 `display_init()` / 切换屏幕函数中统一把所有 label 的 `text_color` 设为 `lv_color_white()`，含中文的 label 统一走 `simsun_16_cjk` |
| **上电瞬间显示 PicoPixel 默认占位值（"9:00" / "2025-01-15" / "Sunny"）** | `ui_init()` 不再调用 `lv_scr_load(badge_main)`，只创建对象；`create_screen_badge_main()` 中所有 label 默认文本设为空字符串 `""`；真实数据由 `display_main_screen()` 写入后才第一次加载屏幕 |
| **唤醒后仍显示 WiFi 连接界面（"正在配置WiFi"）** | 天气数据现在持久化到 NVS（`weather_save_to_nvs` / `weather_load_from_nvs`）；`app_main()` 改为 `while(1)` 无限循环，唤醒后检查数据新鲜度（<1小时），新鲜则直接 `enter_display_mode()`，不再走 `enter_update_mode()` |
| **唤醒后 UART 日志不输出** | `power_enable_light_sleep()` 从 `power_init()` 移到 `enter_low_power_mode()`；新增 `power_disable_light_sleep()` 在 GPIO 唤醒路径调用，恢复 CPU 160MHz 全速；加 50ms 延迟确保系统从 light sleep 完全恢复 |
| **中文天气描述显示方框（如"多云"的"云"）** | `get_weather_name()` 增加 `font_supports_chinese()` 检查：字库支持全部字符才返回中文，否则返回英文 fallback；同时整理了 simsun_16_cjk 完整支持字符列表（见 `docs/simsun_16_cjk_chars.txt`） |

---

## 8. TODO / 待办功能

> 原则：**硬件相关功能（背光控制、低功耗、外接传感器等）暂不列入 TODO**。以下全部是纯软件/纯 UI 可做的改进。

### 8.1 高优先级（小改动）

- [ ] 更新时间的 UI 文案：当前 `2026.6.9 10:35更新` 可改成 `更新于 10:35`（今天）/ `昨天 10:35` / `6月9日 10:35`，更紧凑好读
- [x] ~~温度显示加上 °C 符号~~ ✅ **已完成**：montserrat_18 字体包含 U+00B0（°），代码中直接写 `"%d°C"`
- [x] ~~日期/时间格式统一~~ ✅ **已完成**：日期格式为 `YYYY.M.D`，时间格式为 `HH:MM  周X`
- [ ] 屏保时间字号可以再加大（当前 48px，240 屏能塞下更大的字号）

### 8.2 中优先级（需要改 UI/逻辑）

- [ ] 天气代码更完整的图标与动画映射：当前 101 多云、100 晴、300 雨等都有对应图标，但极端天气（冰雹、霾、沙暴等）可能没有素材，需要补图或回退到文字显示
- [ ] `icon_rotation_correction()` 中可能还藏着其他天气代码的旋转错位问题（目前只确认并修复了 101 多云）——逐个验证
- [ ] 在显示模式下，显示的湿度/风力实际上是"上次更新时"的值，不是实时值，可以加一行小字说明（如"已过时 N 分钟"）
- [ ] 刷新按键后如果天气 API 失败，目前是静默回退到旧数据，应在 UI 上闪一下"更新失败"提示
- [ ] NTP 超时优化：当前 3s×2，若 NTP 连不通（路由器屏蔽 123/UDP）就白等。可在有天气 API 时间后完全跳过 NTP
- [ ] WiFi 失败后日志有"附近 AP 扫描"功能，可做"自动选择信号更强的已保存 AP"逻辑

### 8.3 低优先级（体验优化 / 新功能）

- [ ] 屏保增加一个极简模式（例如只显示一个 `MM` 两位分钟数 + 点状图形），长期显示时更省电/防烧屏
- [ ] 动画调速：当前天气图标动画周期写死在代码（6秒），可加到 config.h
- [ ] UTC 夏令时/冬令时切换逻辑（中国不使用夏令时，暂不需要；但若出国或切换到非中国地区要处理）
- [ ] WPA3 支持（如果路由器开启 WPA3 过渡模式，当前 STA 连接是否正常？需要实测试）

### 8.4 文档 / 代码清理

- [ ] `config.h` 中的 `DEBUG_MODE` 未被 main.c 以外的模块使用，可以移除
- [ ] 清理 `weather.c` 中未使用的字段（若有）
- [ ] `display.c` 中 `is_initialized` 状态位目前只在 `display_deinit` 校验，`display_init` 也应当检查防止重复初始化

---

## 9. 配置参数速查

| 参数 | 值 | 位置 |
|------|-----|------|
| WiFi SSID | CONFIG_WIFI_SSID / NVS 保存 | sdkconfig / NVS |
| WiFi Password | CONFIG_WIFI_PASSWORD / NVS 保存 | sdkconfig / NVS |
| 单配置连接超时 | 20000 ms | [config.h:75](file:///E:/HouYawei/01.ESP32Project/Electronic_Badge/main/include/config.h#L75-L75) |
| 单配置重连次数 | 10 | [config.h:76](file:///E:/HouYawei/01.ESP32Project/Electronic_Badge/main/include/config.h#L76-L76) |
| SmartConfig 超时 | 5 min | [config.h:79](file:///E:/HouYawei/01.ESP32Project/Electronic_Badge/main/include/config.h#L79-L79) |
| 显示模式超时（进入低功耗） | 60 s | main.c `DISPLAY_TIMEOUT` |
| TIMER 唤醒间隔 | 1 min | main.c `LIGHT_SLEEP_INTERVAL_US` (= 60s) |
| 天气数据过期判断 | 1 hour | main.c `while(1)` 循环中的 `> 3600` |
| 天气 API Host | devapi.qweather.com | [config.h:87](file:///E:/HouYawei/01.ESP32Project/Electronic_Badge/main/include/config.h#L87-L87) |
| 天气 API Key | CONFIG_WEATHER_API_KEY | sdkconfig |
| Location Code | 101180714（南阳卧龙） | [config.h:99](file:///E:/HouYawei/01.ESP32Project/Electronic_Badge/main/include/config.h#L99-L99) |
| SPI 时钟 | 20 MHz | sdkconfig |
| 时区 | CST-8 (UTC+8) | main.c |
| 电源管理 min_freq | 40 MHz（低功耗模式） | power.c |
| 电源管理 max_freq | 160 MHz（全速模式） | power.c |

---

## 10. 最近变更记录（2026-06-12）

### 10.1 电源管理重构：Deep Sleep → Light Sleep

**原因**：RST 引脚（GPIO5）与背光供电共享。Deep sleep 时 GPIO 被拉低，导致屏幕复位，唤醒后需重新初始化。

**变更**：
- 废弃 deep sleep，改用 **light sleep**
- `power_init()` 只配置按键 GPIO，**不**启用 `esp_pm_configure(.light_sleep_enable = true)`
- 新增 `power_enable_light_sleep()`：在 `enter_low_power_mode()` 中调用，启用 tickless idle
- 新增 `power_disable_light_sleep()`：在 GPIO 唤醒路径调用，恢复 CPU 160MHz 全速
- 效果：启动/显示/更新阶段日志正常输出；屏保阶段自动省电

**相关文件**：`power.c`, `power.h`, `main.c`

### 10.2 天气数据 NVS 持久化

**原因**：天气数据只存 RAM，重启/唤醒后丢失，导致每次都显示 WiFi 连接界面。

**变更**：
- 新增 `weather_save_to_nvs()` / `weather_load_from_nvs()`
- `app_main()` 初始化时从 NVS 加载天气数据
- `enter_update_mode()` 成功获取天气后保存到 NVS
- `app_main()` 改为 `while(1)` 无限循环，根据数据新鲜度（<1小时）判断是否需要更新

**相关文件**：`weather.c`, `weather.h`, `main.c`

### 10.3 消除 PicoPixel 默认占位 UI

**原因**：上电瞬间屏幕显示 "9:00" / "2025-01-15" / "Sunny" 等 PicoPixel 原始默认值，然后才被真实数据覆盖。

**变更**：
- `ui_init()` 不再调用 `lv_scr_load(badge_main)`，只创建 LVGL 对象
- `create_screen_badge_main()` 中 6 个 label 的默认文本改为空字符串 `""`
- `display_main_screen()` 写入真实数据后才第一次调用 `loadScreen()`
- 效果：上电后屏幕保持黑屏（空文本），真实数据写入后直接显示完整内容

**相关文件**：`ui.c`, `screens.c`, `display.c`

### 10.4 ℃ 符号显示

**实现**：温度标签使用 montserrat_18 字体，该字体生成参数包含 `-r 0xB0`（Unicode U+00B0 = °）。代码中直接写 `"%d°C"`，UTF-8 编码被 LVGL 正确解码渲染。

**相关文件**：`display.c`

### 10.5 中文星期显示

**实现**：`wday_name()` 将 `tm_wday` 映射为 `"周日"`~`"周六"`，时间标签使用 simsun_16_cjk 字体渲染。

**相关文件**：`display.c`

### 10.6 中文天气描述 Fallback

**实现**：`get_weather_name()` 增加 `font_supports_chinese()` 检查。字库不支持全部字符时返回英文（如 `"Cloudy"`），避免显示方框。

**相关文件**：`display.c`, `font_chars.c`

### 10.7 sdkconfig.defaults 清理

**变更**：删除已弃用的 Kconfig 符号（`CONFIG_APP_OFFSET`、`CONFIG_OPTIMIZATION_LEVEL_DEBUG` 等），使用 IDF v5.x 等效符号。

**相关文件**：`sdkconfig.defaults`
