# 电子吧唧程序逻辑设计文档

## 1. 硬件配置

### 1.1 GPIO定义

> **硬件版本说明**：
> - **调试版(经典款)**：带串口芯片(CH340/CP2102)，GPIO20被串口占用，使用GPIO0作为SPI_SCK
> - **量产版(简约款)**：不带串口芯片，GPIO20可用作SPI_SCK
> - 当前配置为**调试版**，量产时请修改 `GPIO_SPI_SCK` 为 `20`

| 功能 | GPIO | 说明 | 版本 |
|------|------|------|------|
| SPI_SCK (SCL) | **GPIO0** | 屏幕SPI时钟 | 调试版 |
| SPI_SCK (SCL) | GPIO20 | 屏幕SPI时钟 | 量产版 |
| SPI_MOSI (SDA) | GPIO10 | 屏幕SPI数据 | 通用 |
| LCD_DC | GPIO7 | 屏幕数据/命令选择 | 通用 |
| LCD_CS | GPIO6 | 屏幕片选 | 通用 |
| LCD_RST | GPIO5 | 屏幕复位（同时控制背光供电） | 通用 |
| LCD_BL | — | 未引出到ESP32-C3（由RST控制背光） | 通用 |
| BTN_WAKE | GPIO4 | 唤醒/休眠键，GPIO唤醒源 | 通用 |
| BTN_REFRESH | GPIO3 | 刷新/配网键 | 通用 |

> **注意**：RST（GPIO5）同时控制屏幕复位和背光供电。

### 1.2 唤醒源配置
- **GPIO唤醒**: GPIO4 (WAKE键)，低电平触发（使用 `esp_deep_sleep_enable_gpio_wakeup()`）
- **TIMER唤醒**: 可配置间隔（默认30分钟，通过SDK配置编辑器设置）

---

## 2. 状态机设计

```
┌─────────────────────────────────────────────────────────────────┐
│                        上电/复位                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  【INIT】初始化阶段                                              │
│  1. 初始化NVS                                                   │
│  2. 初始化GPIO（按键）                                          │
│  3. 读取唤醒原因                                                 │
│  4. 初始化显示（SPI + GC9A01 + LVGL）                           │
│  5. 根据唤醒原因跳转                                             │
└─────────────────────────────────────────────────────────────────┘
          │
          ├─────────────────────┬─────────────────────┐
          ▼                     ▼                     ▼
┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐
│  TIMER_WAKEUP   │   │   GPIO_WAKEUP   │   │  POWERON_RESET  │
│   (定时更新)     │   │   (按键唤醒)     │   │   (首次上电)     │
└────────┬────────┘   └────────┬────────┘   └────────┬────────┘
         │                     │                     │
         ▼                     ▼                     ▼
┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐
│ 【UPDATE】更新模式 │   │【DISPLAY】显示模式│   │ 【DISPLAY】显示模式│
└─────────────────┘   └─────────────────┘   └─────────────────┘
```

---

## 3. 各模式详细逻辑

### 3.1 【INIT】初始化阶段

```c
void init_phase() {
    // 1. 初始化NVS
    nvs_flash_init();
    
    // 2. 初始化GPIO
    gpio_config_t io_conf = {};
    // 配置按键输入（内部上拉）
    // 配置RST输出（默认低电平，关闭屏幕和背光）
    
    // 3. 读取唤醒原因
    esp_sleep_wakeup_cause_t cause = esp_sleep_get_wakeup_cause();
    
    // 4. 初始化显示
    display_init();
    display_backlight_on();
    
    // 5. 根据原因跳转
    switch(cause) {
        case ESP_SLEEP_WAKEUP_TIMER:
            enter_update_mode();
            break;
        case ESP_SLEEP_WAKEUP_GPIO:  // GPIO唤醒（WAKE键）
            enter_display_mode();
            break;
        default: // POWERON_RESET
            enter_display_mode();
            break;
    }
}
```

---

### 3.2 【DISPLAY】显示模式

**进入条件**: GPIO唤醒 或 首次上电

**功能**: 显示主屏幕（纯黑背景 + 时间 + 天气 + 温度）

**UI布局**:
```
┌─────────────────────────────┐
│                             │
│        08:44                │  ← 时间（白色，Montserrat 14px）
│                             │
│          雾                  │  ← 天气名称（浅蓝，思源黑体 16px）
│                             │
│         27C                 │  ← 温度（白色，思源黑体 16px）
│                             │
│      更新 08:30             │  ← 更新时间（灰色，思源黑体 14px）
│                             │
└─────────────────────────────┘
  背景：纯黑 #000000
```

**显示技术栈**:
- **驱动**: esp_lcd + esp_lcd_gc9a01（SPI 接口）
- **GUI**: esp_lvgl_port + LVGL v9
- **字体**: lv_font_montserrat_14（数字/英文）+ lv_font_source_han_sans_sc_16_cjk（中文子集）
- **颜色格式**: RGB565，`swap_bytes = true`（关键！解决雪花纹）
- **缓冲**: 单缓冲 10% 屏幕大小（ESP32-C3 RAM 不足，无法双缓冲）

**按键处理**:

| 按键 | 操作 | 动作 |
|------|------|------|
| WAKE | 短按 | 无动作（已在显示模式） |
| WAKE | 长按(>2秒) | 进入休眠 |
| REFRESH | 短按 | 进入【REFRESH】刷新模式 |
| REFRESH | 长按(>3秒) | 进入【CONFIG】配网模式 |

**退出条件**:
- 长按WAKE → 进入【SLEEP】休眠
- 短按REFRESH → 进入【REFRESH】刷新模式
- 长按REFRESH → 进入【CONFIG】配网模式
- 超时无操作(60秒) → 进入【SLEEP】休眠

---

### 3.3 【UPDATE】更新模式

**进入条件**: TIMER唤醒 或 首次启动

**功能**: 后台更新天气数据，可选生成AI图片

**流程**:
```
1. 初始化显示（显示加载动画）
2. 尝试连接WiFi（按优先级）
   - 优先尝试sdkconfig默认配置（CONFIG_WIFI_SSID/CONFIG_WIFI_PASSWORD）
   - sdkconfig未设置或连接失败时，尝试NVS保存的配置（SmartConfig配网，最多5组）
3. NTP同步时间
4. 显示主界面（使用RTC缓存数据，用户不用等待）
5. 后台获取天气数据（和风天气API）
6. 保存天气数据到RTC Memory
7. 静默刷新主界面（更新天气数据）
8. 保持WiFi连接，返回成功状态
```

> **注意**：更新模式不关闭WiFi和显示，由调用者决定后续流程。定时唤醒后更新成功会进入显示模式让用户看30秒。

---

### 3.4 【REFRESH】刷新模式

**进入条件**: 在显示模式下短按REFRESH键

**功能**: 立即更新天气数据

**流程**:
```
1. 检查WiFi是否已连接
   - 已连接：直接获取天气（秒刷新）
   - 未连接：显示"配置WIF" → 连接WiFi → 同步时间 → 显示主界面
2. 获取最新天气
3. 刷新屏幕显示
4. 保持WiFi连接
5. 重置60秒休眠倒计时
```

> **注意**：刷新后保持WiFi连接，下次刷新无需重新连接，实现秒刷新。

---

### 3.5 【CONFIG】配网模式

**进入条件**: 在显示模式下长按REFRESH键(>3秒)

**功能**: 配置WiFi连接

**流程**:
```
1. 显示"配网模式"提示
2. 启动SmartConfig (一键配网)
3. 等待手机配置（使用微信小程序"一键配网"）
4. 收到配置后：
   - 自动尝试连接新WiFi
   - 连接成功：保存到NVS（支持最多5组配置）
   - 显示"同步成功"
   - 自动进入UPDATE模式获取天气
5. 退出方式：
   - 配网成功 → 自动更新天气 → 进入DISPLAY模式
   - 长按WAKE键(>2秒) → 退出配网 → 回到显示模式
   - 超时(5分钟) → 回到显示模式
```

**WiFi配置优先级**:

| 优先级 | 配置来源 | 使用场景 |
|--------|---------|---------|
| 1 | **sdkconfig默认配置** | 开发调试/固定环境 |
| 2 | **NVS保存的配置** | 正常使用/多环境切换 |

---

### 3.6 【SLEEP】休眠模式

**进入条件**: 
- 显示模式超时（60秒无操作）
- 长按WAKE键

**流程**:
```
1. 断开WiFi（降低功耗）
2. 显示屏保（黑色背景 + 大号时间 + "Press WAKE"提示）
   - 硬件限制无法关闭背光，关闭显示无意义
   - 屏保显示静态内容，防止烧屏
3. 配置GPIO唤醒源（GPIO4，低电平）
4. 配置TIMER唤醒源（默认30分钟）
5. 保存必要状态到RTC Memory
6. 进入Deep Sleep（显示保持屏保内容）
```

> **注意**：由于硬件设计限制，背光无法通过软件关闭。ESP32休眠后屏幕保持显示屏保内容，功耗主要来自背光（约20-40mA）。这是硬件限制，软件无法优化。

---

## 4. 数据存储设计

### 4.1 NVS存储（Flash，持久保存）

| 键名 | 类型 | 说明 |
|------|------|------|
| wifi_config_0~4 | struct | 5组WiFi配置 |

### 4.2 RTC Memory（Deep Sleep保持）

| 变量 | 类型 | 说明 |
|------|------|------|
| last_weather | struct | 上次天气数据 |
| last_update_time | time_t | 上次更新时间 |
| has_ai_image | bool | 是否有AI生成图片 |
| boot_count | uint32 | 启动次数 |

---

## 5. 显示模块

### 5.1 技术架构

```
display.c
├── esp_lcd (底层驱动)
│   ├── SPI 总线初始化 (SPI2_HOST)
│   ├── GC9A01 面板驱动 (自定义初始化序列)
│   └── 硬件控制 (reset, mirror)
│
└── esp_lvgl_port (LVGL 适配层)
    ├── lvgl_port_init()          ← 初始化 LVGL 任务
    ├── lvgl_port_add_disp()      ← 注册显示设备
    ├── lvgl_port_lock/unlock()   ← 线程安全
    └── lvgl_port_remove_disp()   ← 反初始化
```

### 5.2 关键配置

| 配置项 | 值 | 说明 |
|--------|------|------|
| SPI 频率 | 20MHz | 默认，可通过 sdkconfig 调整 |
| 颜色顺序 | RGB + swap_bytes=true | 解决雪花纹的关键 |
| 缓冲区 | 10% 屏幕，单缓冲 | ESP32-C3 RAM 限制 |
| 镜像 | mirror_x=true | 修正显示方向 |
| 初始化序列 | 厂商自定义序列 | 含 Display Inversion ON (0x21) |

### 5.3 字体

| 字体 | 用途 | 备注 |
|------|------|------|
| lv_font_montserrat_14 | 时间、数字 | LVGL 内置，完整支持 |
| lv_font_source_han_sans_sc_14_cjk | 更新时间等小字 | 中文子集，约200-300字 |
| lv_font_source_han_sans_sc_16_cjk | 天气名称、温度等 | 中文子集，约200-300字 |

> **注意**: CJK 子集字体不包含所有汉字，显示文本需使用常用字。生僻字（如"连""图""程""序"等）会显示为方块。

### 5.4 UI 界面列表

| 界面 | 函数 | 内容 |
|------|------|------|
| 主屏幕 | display_main_screen() | 纯黑背景 + 时间 + 天气 + 温度 + 更新时间 |
| 加载中 | display_loading() | 纯黑背景 + 居中白色文字 |
| 配网模式 | display_config_mode() | 纯黑背景 + "配网模式" + 说明文字 |
| 同步成功 | display_config_success() | 纯黑背景 + "同步成功"(绿色) + "WiFi已配置" |

---

## 6. AI图片生成

- **服务**: Pollinations.AI (https://image.pollinations.ai)
- **分辨率**: 240x240
- **存储**: SPIFFS (/spiffs/ai_weather.jpg)
- **超时**: 60秒
- **错误处理**: 402(队列满)/429(限流) 时跳过，不影响主流程

---

## 7. 错误处理

| 错误场景 | 处理方式 |
|----------|----------|
| WiFi连接失败 | 首次启动进入配网模式；非首次使用缓存数据 |
| 天气API失败 | 使用RTC缓存数据显示 |
| 显示反初始化崩溃 | 确保先停止LVGL task再删除硬件资源 |
| 按键抖动 | 软件消抖（10ms） |

---

## 8. 功耗优化策略

1. **Deep Sleep**: 主休眠模式，仅RTC运行
2. **屏幕和背光控制**: RST (GPIO5) 控制屏幕和背光供电，休眠时拉低关闭
3. **WiFi及时关闭**: 休眠前断开WiFi，更新后保持连接以便秒刷新
4. **显示反初始化**: 休眠前完整反初始化LVGL + GC9A01 + SPI
   - 先停止动画和timer
   - 在LVGL task运行时移除显示
   - 停止task并等待退出
   - 最后删除硬件资源

---

## 9. 配置说明

### SDK配置（menuconfig）

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| WiFi SSID | 默认WiFi名称 | 空 |
| WiFi Password | 默认WiFi密码 | 空 |
| 天气API Host | 和风天气API Host | devapi.qweather.com |
| 天气API Key | 和风天气API密钥 | 空 |
| Location Code | 城市代码 | 101180714（南阳卧龙）|
| 更新间隔 | 天气更新间隔 | 30分钟 |
| 显示超时 | 无操作休眠时间 | 60秒 |
| SPI时钟 | SPI频率 | 20MHz |

---

## 10. 代码模块划分

```
main/
├── main.c              // 主程序入口（状态机、按键处理）
├── Kconfig.projbuild   // 菜单配置
├── CMakeLists.txt
├── include/
│   ├── config.h        // 全局配置（从sdkconfig读取）
│   ├── display.h       // 显示模块接口
│   ├── wifi_manager.h  // WiFi管理
│   ├── weather.h       // 天气API
│   ├── button.h        // 按键处理
│   └── power.h         // 电源管理
├── src/
│   ├── display.c       // GC9A01驱动 + LVGL UI（esp_lvgl_port）
│   ├── wifi_manager.c  // WiFi连接 + SmartConfig
│   ├── weather.c       // 和风天气API
│   ├── button.c        // 按键中断 + 消抖
│   └── power.c         // Deep Sleep管理
├── ui/                 // PicoPixel 生成的 LVGL UI
│   ├── src/ui/         // 界面代码（screens.c, ui.c）
│   └── images/         // 天气图标资源
```

---

## 11. 日志TAG对照表

| 模块 | TAG | 文件 |
|------|-----|------|
| 主程序 | "主程序" | main.c |
| 显示 | "显示" | display.c |
| WiFi | "WiFi" | wifi_manager.c |
| 天气 | "天气" | weather.c |
| 按键 | "按键" | button.c |
| SmartConfig | "SmartConfig" | wifi_manager.c |

---

## 12. PicoPixel UI 设计工具使用指南

### 12.1 工具简介

[PicoPixel](https://picopixel.io/zh/) 是一款基于浏览器的 LVGL 可视化编辑器，用于设计嵌入式 UI 界面。

### 12.2 项目设置

| 设置项 | 值 | 说明 |
|--------|------|------|
| 分辨率 | **240 x 240** | GC9A01 圆形屏幕 |
| LVGL 版本 | **8.x** | 与项目一致 |
| 项目名称 | 自定义 | 如 `badge_loading` |

> **注意**：GC9A01 是圆形屏幕，但 LVGL 逻辑区域仍是 240x240 方形。四角约 20px 区域会被遮挡，重要内容应放在中心 200x200 安全区域内。

### 12.3 顶部工具栏

从左到右依次为：

| 图标 | 名称 | 功能 |
|------|------|------|
| 网格点 | **Widgets** | 基础组件面板（Label, Button, Arc 等） |
| 钢笔 | **Draw** | 绘图工具 |
| 六边形 | **Shapes** | 形状工具 |
| 箭头 | **Display** | 显示类组件下拉菜单 |
| 方框 | **Layout** | 布局容器下拉菜单 |
| 图片 | **Assets** | 资源/图片下拉菜单 |
| T | **Input** | 输入类组件下拉菜单 |
| 对话框 | **Interact** | 交互类组件下拉菜单 |

#### Display 下拉菜单
- **Panel** - 面板
- **Container** - 容器
- **Image** - 图片
- **Chart** - 图表
- **Bar** - 进度条

#### Input 下拉菜单
- **Label** - 文字标签
- **Textarea** - 文本输入框
- **Roller** - 滚轮选择器
- **Dropdown** - 下拉菜单

#### Interact 下拉菜单
- **Button** - 按钮
- **Arc** - 弧形/圆弧
- **Slider** - 滑块
- **Switch** - 开关
- **Checkbox** - 复选框
- **Spinbox** - 数字输入框

### 12.4 左侧面板

| 标签页 | 功能 |
|--------|------|
| **图层** | 显示当前界面的组件层级结构 |
| **素材** | 管理图片、字体等资源 |

### 12.5 右侧面板

| 区域 | 功能 |
|------|------|
| **SIZE** | 设置组件尺寸 (W, H) |
| **TRANSFORMATION** | 设置位置 (X, Y) |
| **LAYER** | 图层设置 |
| **SCREEN PROPERTIES** | 屏幕属性（名称、背景色等） |
| **STYLE** | 样式设置（颜色、字体、边框等） |

### 12.6 设计流程

1. **创建项目**：New Project → 设置 240x240 → 选择 LVGL 8.x
2. **设置背景**：选中 Screen → 右侧面板设置背景色 #000000
3. **添加组件**：点击顶部工具栏图标 → 选择组件类型 → 在画布上点击放置
4. **调整样式**：选中组件 → 右侧面板调整位置、大小、颜色、字体
5. **导出代码**：点击右上角 "EXPORT LVGL UI" → 下载 zip

### 12.7 字体处理方案

PicoPixel 内置字体（Noto Sans 等）不支持中文，创建自定义中文字体时会报错。采用以下方案：

| 步骤 | 操作 |
|------|------|
| **PicoPixel 设计** | Label 使用英文占位（如 "Syncing..." / "Time" / "Temp"） |
| **导出后处理** | 代码中动态替换文字为中文，并设置中文字体 |

**代码示例**：
```c
// 加载 PicoPixel 界面后，替换 Label 文字和字体
lv_obj_set_style_text_font(objects.label_1, &lv_font_simsun_16_cjk, 0);
lv_label_set_text(objects.label_1, "同步中...");
```

> **注意**：`lv_font_simsun_16_cjk` 是 LVGL 8.x 内置中文字体（16px），需在 sdkconfig 中启用 `CONFIG_LV_FONT_SIMSUN_16_CJK=y`。

### 12.8 界面清单

| # | 界面名称 | 文件名 | 内容 | 状态 |
|---|---------|--------|------|------|
| 1 | **加载中** | `badge_loading` | 旋转动画 + "Syncing..." → "同步中..." | ✅ 已完成 |
| 2 | **主屏幕** | `badge_main` | 时间 + 天气 + 温度 + 详细信息 | ✅ 已完成 |
| 3 | **配网模式** | `badge_config` | 二维码 + "Config Mode" → "配网模式" | ✅ 已完成 |
| 4 | **同步成功** | `badge_success` | 成功提示 + "Success" → "配置成功" | ✅ 已完成 |

### 12.9 界面中文替换映射

| 界面 | 英文占位 | 中文替换 | 对象名 |
|------|---------|---------|--------|
| **badge_loading** | `Syscing...` | `同步中...` | `label_1` |
| **badge_success** | `Success` | `配置成功` | `label_2` |
| **badge_success** | `Press any key` | `按任意键继续` | `label_3` |
| **badge_config** | `Config Mode` | `配网模式` | `label_4` |
| **badge_config** | `Wake` | `唤醒` | `label_5` |
| **badge_config** | `Wechat` | `微信` | `label_6` |
| **badge_config** | `Scan` | `扫码` | `label_7` |
| **badge_main** | `9:00` | 动态时间 | `label_time` |
| **badge_main** | `2025-01-15` | 动态日期 | `label_date` |
| **badge_main** | `Sunny` | 天气中文/英文 | `label_weather` |
| **badge_main** | `25C` | 动态温度 | `label_temp` |
| **badge_main** | `Hum:60% Wind:3` | `Hum:60% Wind:3` | `label_detail` |
| **badge_main** | `Updated` | `2026.6.8 19:45更新` | `label_update` |
